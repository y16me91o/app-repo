apiVersion: v1
kind: Service
metadata:
  name: yourapp
  namespace: yourapp
spec:
  type: NodePort
  selector:
    app: yourapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
